// Layout stubs to prevent import errors
import React from 'react'

export const AuthLayout = ({ children }: any) => React.createElement('div', {}, children)
export const DashboardLayout = ({ children }: any) => React.createElement('div', {}, children)
export const RootLayout = ({ children }: any) => React.createElement('div', {}, children)
